<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"
    tools:context=".views.home.HomeActivity">


    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/cl_tab_view"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@color/white"
        android:padding="5dp"
        app:layout_constraintBottom_toBottomOf="parent">
        <!--Tab layout-->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tab_layout_movies"
            style="@style/CustomTabLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/white"
            android:contentDescription="@string/tab_layout_description"
            app:layout_constraintBottom_toBottomOf="parent"
            app:tabIndicator="@drawable/sm_selected_tab_bg"
            app:tabIndicatorAnimationMode="elastic"
            app:tabIndicatorColor="#F5F5F5"
            app:tabIndicatorGravity="stretch"
            app:tabInlineLabel="false"
            app:tabMode="fixed"
            app:tabRippleColor="@color/transparent"
            app:tabSelectedTextColor="@color/tabs_text_icon"
            app:tabTextColor="@color/light_gray" />

    </androidx.constraintlayout.widget.ConstraintLayout>


    <!--View pager for fragments-->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/view_pager_movies"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginBottom="2dp"
        android:background="@color/white"
        app:layout_constraintBottom_toTopOf="@+id/cl_tab_view"
        app:layout_constraintTop_toTopOf="parent" />

    <!--Frame Layout to include fragments -->
    <FrameLayout
        android:id="@+id/tile_fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />


</androidx.constraintlayout.widget.ConstraintLayout>